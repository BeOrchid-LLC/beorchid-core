# core-api

BeOrchid Core — System 1: Identity + Central Database.

Implements the approved architecture in
`../BeOrchid-Core-System-1-Architecture_final.md`. Section references
throughout the code point back to it.

## Status

Build log for this slice: [`docs/build-log.md`](docs/build-log.md).

Milestone 2, first slice: the `core` schema, the permission-resolution
safeguards, and the least-privilege grant model — with tests that prove the
guarantees rather than assert them in prose.

| Section 16 acceptance row | State |
|---|---|
| `core` schema live, migrations versioned, both views returning filtered results | Done (local) |
| Per-app DB roles — zero access to `core` verified by test | Done (local) |
| Clerk configured, both environments | Not started — needs Clerk instances |
| Core API deployed | Not started |
| Access logging | Table + grants done; API write path not started |
| Reference apps | Not started |
| Staging + production | Not started — needs Contabo/Coolify |
| Backups, monitoring | Not started |

## Requirements

- Node 22 (`.nvmrc`)
- PostgreSQL **15 or newer** — `UNIQUE NULLS NOT DISTINCT` is load-bearing
  (see "Deviations" below). Developed against 16.

## Local setup

Full instructions, prerequisites and troubleshooting live in
[`../SETUP.md`](../SETUP.md). The short version, once those prerequisites are
met:

```bash
nvm use
npm install
createdb beorchid_core_dev
cp .env.example .env
npm run db:migrate      # applies migrations 0000-0004
npm run db:bootstrap    # grants LOGIN + local dev passwords
npm test                # expect 24 passing
```

`db:bootstrap` is local-only and refuses to run with `NODE_ENV` set to anything
but `development`. In staging and production the equivalent step is Coolify
injecting per-environment credentials from Infisical (Section 12).

## Migrations

Forward-only and versioned (Section 9.3). An applied migration is never edited
— migration `0004` exists precisely because `0003` was already applied.

| File | Contents |
|---|---|
| `0000_extensions.sql` | `pgcrypto`, `citext` |
| `0001_core_schema.sql` | The ten `core` tables (generated by Drizzle Kit) |
| `0002_resolution_views.sql` | The two safeguard views, hand-written |
| `0003_roles_and_grants.sql` | Database roles and least-privilege grants |
| `0004_migrate_role_db_grant.sql` | `CREATE` on database for the migration role |

Tables are generated from `src/db/schema/`. Views and grants are hand-written
SQL on purpose: they are the enforcement mechanism, so the exact statements
should be reviewable as SQL rather than reconstructed from an ORM builder.

## How enforcement actually works

Section 5.2 requires that Core API resolve permissions only through the
filtered views, and that `core.role_permissions` stay unreachable for that
purpose. Three mechanisms combine:

1. The views carry the app filter, written once.
2. The views are `security_invoker = false`, so they execute with their
   owner's privileges.
3. `core_api_rw` holds **no privilege at all** on `core.permissions` or
   `core.role_permissions`.

The result: resolution through a view succeeds, and a hand-rolled join that
forgets the app filter fails with `42501 insufficient_privilege`. It is not a
convention a future engineer can quietly break.

Two database roles serve Core API, because "reachable for administration but
not for resolution" cannot be expressed as one role:

- `core_api_rw` — request-serving. Views only.
- `core_api_admin` — role/permission administration. Direct table access.

## Deviations from the document, and open items

**`UNIQUE NULLS NOT DISTINCT` on `core.permissions`.** Section 5.2's DDL uses a
plain `unique (app_id, key)`. Postgres treats NULLs as distinct in a unique
constraint, so that constraint accepts `('members:invite', NULL)` twice —
duplicate core-wide permissions, the exact thing it exists to prevent. Raised
to `NULLS NOT DISTINCT`, which requires PG 15+. Covered by a test.

**Audit log is append-only to runtime.** `core_api_rw` gets `INSERT` and
`SELECT` on `core.access_log`, not `UPDATE` or `DELETE`. Not specified in
Section 6.5; added because an audit trail a compromised runtime can rewrite is
not an audit trail. Covered by a test.

**No `DEFAULT PRIVILEGES` on schema `core`.** A future table starts unreachable
by every role and must be granted explicitly. A forgotten grant fails loudly;
a silently inherited one would not.

**Per-app role naming is unconfirmed.** Section 15.2 flags `<app>_rw` as the
one convention not yet signed off. Used throughout as a placeholder and marked
at each site. Confirm before the first app role is created in staging.

## Known advisories

`npm audit` reports 4 moderate findings, all `esbuild` reached through
`drizzle-kit`, a devDependency that is never deployed. `npm audit fix --force`
resolves them by *downgrading* drizzle-kit to 0.18.1, which is worse. Left as
is deliberately.

## Not yet built

Core API HTTP surface (Section 3.1a), Clerk webhook ingest (Section 4.6),
Redis caching and its fail-safe behaviour (Section 11), the Core SDK
(Section 5.6), and both reference apps.
